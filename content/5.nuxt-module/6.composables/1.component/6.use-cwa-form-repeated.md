---
title: "useCwaFormRepeated"
navigation:
  title: "Form Repeated"
description: Bind a Symfony RepeatedType field (e.g. new password + confirmation) with cross-field validation.
---

`useCwaFormRepeated` wraps two [`useCwaFormInput`](/nuxt-module/composables/component/use-cwa-form-input) instances for a Symfony [`RepeatedType`](https://symfony.com/doc/current/reference/forms/types/repeated.html) field. Each side validates with the sibling's current value so the API can check that both entries match.

```ts
const { first, second } = useCwaFormRepeated(iri, 'form_name[fieldName]')
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `iri` | `Ref<string \| undefined>` | IRI of the Form component resource |
| `fullName` | `string` | `full_name` of the RepeatedType field (e.g. `registration[plainPassword]`) |

## Return value

Returns `{ first, second }` — each has the same shape as a [`useCwaFormInput`](/nuxt-module/composables/component/use-cwa-form-input) return value:

| Property | Type | Description |
|----------|------|-------------|
| `value` | `Ref<string>` | Field value; bind with `v-model="field.value.value"` |
| `vars` | `ComputedRef` | Field vars from the API (`label`, `required`, etc.) |
| `errors` | `ComputedRef<string[]>` | Field-level errors |
| `valid` | `ComputedRef<null \| boolean>` | Validation state |
| `displayErrors` | `ComputedRef<boolean>` | Whether to show errors (gated by blur / regression / submitAttempted) |
| `onBlur` | `() => void` | Call on the input's `blur` event |
| `onInput` | `() => void` | Debounced; triggers cross-field validation |
| `validate` | `(extraData?) => Promise<void>` | Explicit validate (PATCH forms only) |

The `onInput` and `onBlur` handlers are overridden to pass the sibling's current value as `extraData` — this lets the API validate that both entries match without requiring the user to blur both fields.

## Example

```vue
<script setup lang="ts">
const props = defineProps<{ iri: string }>()
const iriRef = toRef(props, 'iri')
const password = useCwaFormRepeated(iriRef, 'registration[plainPassword]')
</script>

<template>
  <UFormField
    :label="password.first.vars.value?.label || 'New Password'"
    :error="password.first.displayErrors.value ? password.first.errors.value[0] : undefined"
  >
    <UInput
      v-model="password.first.value.value"
      type="password"
      autocomplete="new-password"
      @blur="password.first.onBlur"
      @input="password.first.onInput"
    />
  </UFormField>

  <UFormField
    :label="password.second.vars.value?.label || 'Confirm Password'"
    :error="password.second.displayErrors.value ? password.second.errors.value[0] : undefined"
  >
    <UInput
      v-model="password.second.value.value"
      type="password"
      autocomplete="new-password"
      @blur="password.second.onBlur"
      @input="password.second.onInput"
    />
  </UFormField>
</template>
```

## Notes

- Internally maps to `{fullName}[first]` and `{fullName}[second]` — these match Symfony's RepeatedType child names
- If neither field has a value yet, the sibling is sent as `'__FAKE__'` to avoid a false mismatch on first keystroke
- Each side registers its own `full_name` in the form store via `useCwaFormInput` — `useCwaForm.unregisteredFieldErrors` will not include them
- See the [Forms guide](/nuxt-module/component-helpers/forms) for full context
