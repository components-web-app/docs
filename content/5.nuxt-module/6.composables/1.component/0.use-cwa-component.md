---
title: "useCwaComponent"
navigation:
  title: "Component (recommended)"
description: The recommended entry point for all CWA display components — fetch resource data, expose to the admin manager, and compose plugins for collections, images, or custom behaviour.
badge:
  label: Draft
  color: amber
---

The recommended composable for all CWA display components. Wraps `useCwaResource` and adds a typed plugin system so collection, image, and custom behaviours can be composed together without nesting.

```ts
const props = defineProps<IriProp>()
const { resource, exposeMeta } = useCwaComponent(props)
defineExpose(exposeMeta)
```

## Signature

```ts
useCwaComponent(
  props: IriProp,
  plugins?: CwaResourcePlugin[],
  ops?: {
    name?: string
    styles?: {
      multiple?: boolean
      classes: Record<string, string[]>
    }
    manager?: {
      disabled?: boolean
    }
    autoClass?: boolean   // default true — auto-applies uiClassNames to the root element
  }
)
```

**`props`** — pass the raw `defineProps<IriProp>()` result directly (no `toRef` needed).

**`plugins`** — optional array of plugin factory results (see below).

**`ops`** — same options as `useCwaResource`: style variants, UI variant name, manager opt-out.

## Return values

```ts
const { resource, exposeMeta, $cwa, getCurrentStyleName, ...pluginReturns } = useCwaComponent(props)
```

| Return | Type | Purpose |
|--------|------|---------|
| `resource` | `ComputedRef<Resource \| null \| undefined>` | Reactive resource ref — access data on `.value?.data` |
| `exposeMeta` | object | Pass directly to `defineExpose` |
| `$cwa` | CwaComposable | Auth, resources, UI state |
| `getCurrentStyleName` | `(data) => string \| undefined` | Read the currently selected style name |
| `uiClassNames` | `ComputedRef<string[] \| undefined>` | The active style classes — auto-applied to the root element by default |
| `publishedIri` | `ComputedRef<string \| undefined>` | The published/live IRI of this component — resolves to the published equivalent when the current resource is a draft. Use as the `location` for a nested [`<CwaComponentGroup>`](/nuxt-module/cwa-components/cwa-component-group) owned by this component |
| `...pluginReturns` | merged object | All return values from provided plugins |

**`defineExpose(exposeMeta)`** is **required** — without it the admin manager cannot select this component.

## Basic component

```vue
<template>
  <div v-if="resource?.data">
    <h2>{{ resource.data.title }}</h2>
  </div>
</template>

<script setup lang="ts">
import type { IriProp } from '#cwa/composables/cwa-resource'
import { useCwaComponent } from '#imports'

const props = defineProps<IriProp>()
const { resource, exposeMeta } = useCwaComponent(props)
defineExpose(exposeMeta)
</script>
```

`resource.value` states:
- `undefined` — loading
- `null` — not found
- object — data available

---

## Plugins

Plugins extend `useCwaComponent` with additional state and methods. Pass an array of plugin factory results as the second argument. Return values from all plugins are merged and destructured alongside the base returns.

### `withCollection()`

For components backed by the built-in `Collection` API resource. Returns pagination state and navigation helpers.

```ts
import { useCwaComponent, withCollection } from '#imports'

const props = defineProps<IriProp>()
const {
  resource,
  exposeMeta,
  collectionItems,
  isLoadingCollection,
  totalPages,
  pageModel,
  goToNextPage,
  goToPreviousPage,
  changePage,
  resolveResourceLink,
} = useCwaComponent(props, [withCollection()])
defineExpose(exposeMeta)
```

Replaces the deprecated `useCwaCollectionResource`.

### `withFile(fileOps?)`

For components backed by a PHP entity with `#[Silverback\Uploadable]`. Handles **any** uploadable file, not just images. Rather than flat return values, it exposes the field under a **`files` map keyed by its `fileProp`** (default `'file'`) — so `files.file.contentUrl`, `files.file.displayMedia`, etc. Map entries are `reactive`, so in templates you access them **without `.value`**.

```ts
import { useCwaComponent, withFile } from '#imports'

const props = defineProps<IriProp>()
const { resource, exposeMeta, files } = useCwaComponent(props, [withFile()])
defineExpose(exposeMeta)

// files.file.contentUrl · files.file.displayMedia · files.file.handleLoad · files.file.loaded
```

Options (`fileOps`):
| Option | Description |
|--------|-------------|
| `fileProp` | Name of the PHP `#[UploadableField]` property holding the file (default: `file`). Also the default template-ref name for load detection |
| `imagineFilterName` | Imagine filter variant to surface as `displayMedia`/`contentUrl` (image-specific) |
| `imageRef` | Template ref for the `<img>` element (defaults to `useTemplateRef(fileProp)`) |

Each entry in `files` exposes `contentUrl`, `displayMedia` (a `MediaFile` with `width`/`height`/`mimeType`), `handleLoad`, and `loaded`. See [Images & Media](/nuxt-module/component-helpers/images-and-uploads) for the full display pattern, and [`useCwaFileField`](/nuxt-module/composables/component/use-cwa-file-field) for the standalone per-field variant.

::callout{icon="i-heroicons-arrow-path"}
Renamed from `withImage()` (removed, along with `useCwaImage`/`useCwaImageResource`/`ImageOpsType`). The old plugin returned flat keys; `withFile` returns the `files` map instead.
::

#### Multiple file fields

Use `withFile()` once per field, each with its own `fileProp`. `useCwaComponent`'s plugin merge **accumulates** the `files` key rather than overwriting it, so the fields never collide:

```ts
const { files } = useCwaComponent(props, [
  withFile({ fileProp: 'heroImage' }),
  withFile({ fileProp: 'thumbnail', imagineFilterName: 'small' }),
])
// files.heroImage.contentUrl · files.thumbnail.contentUrl
```

### Combining plugins

Multiple plugins can be composed in a single call — something the old `useCwaCollectionResource` / `useCwaImageResource` pattern could not do:

```ts
const { resource, exposeMeta, collectionItems, files } = useCwaComponent(
  props,
  [withCollection(), withFile()]
)
defineExpose(exposeMeta)
```

---

## Style variants

```ts
const { resource, exposeMeta, getCurrentStyleName } = useCwaComponent(props, [], {
  styles: {
    multiple: false,
    classes: {
      'Default': [],
      'Filled': ['bg-blue-600', 'text-white', 'px-6', 'py-2', 'rounded-md'],
      'Outlined': ['border', 'border-blue-600', 'text-blue-600', 'px-6', 'py-2'],
    }
  }
})

const currentStyleName = computed(() => {
  if (!resource.value?.data) return undefined
  return getCurrentStyleName(resource.value.data)
})
```

The style names appear in the admin "Style" selector. The selected classes are automatically applied to the component's root element — no `:class` binding needed.

### Applying classes to an inner element

If you want the style classes on an inner element rather than the root, opt out of auto-apply and bind `uiClassNames` manually:

```ts
const { resource, exposeMeta, uiClassNames } = useCwaComponent(props, [], {
  autoClass: false,
  styles: { ... }
})
```

```vue
<template>
  <div>
    <a :class="uiClassNames">{{ resource?.data?.label }}</a>
  </div>
</template>
```

---

## Writing a custom plugin

A plugin is a function that receives `{ iri, resource, $cwa }` and returns any object:

```ts
import type { CwaResourcePlugin } from '#cwa/composables/cwa-component'

const withWordCount = (): CwaResourcePlugin<{ wordCount: ComputedRef<number> }> => {
  return ({ resource }) => {
    const wordCount = computed(() => {
      const body = resource.value?.data?.body ?? ''
      return body.split(/\s+/).filter(Boolean).length
    })
    return { wordCount }
  }
}

// Usage
const { resource, exposeMeta, wordCount } = useCwaComponent(props, [withWordCount()])
```
