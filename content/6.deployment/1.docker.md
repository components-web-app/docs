---
title: Docker
description: The Docker Compose template for local development and production, including all required services and environment variable configuration.
---

<!--
COVER:
- The template repository includes a full Docker Compose setup out of the box
  (Clone the template and you have a working local environment in minutes)

- Services in the Compose stack:
    - php: FrankenPHP (serves the Symfony API)
    - nuxt: Node 20 (serves the Nuxt app in SSR mode)
    - database: PostgreSQL 16 (or MySQL — template supports both)
    - mercure: Caddy with Mercure module (real-time hub)
    - traefik: reverse proxy (routes api.* and www.* to the right container)

- File structure:
    docker-compose.yml          — production-like base services
    docker-compose.override.yml — local dev overrides (volume mounts, debug, etc.)

- The key environment variables (.env, .env.local — NEVER commit .env.local):
    DATABASE_URL=postgresql://user:pass@database:5432/app
    JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
    JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
    JWT_PASSPHRASE=your_passphrase
    MERCURE_URL=http://mercure/.well-known/mercure
    MERCURE_PUBLIC_URL=https://mercure.yourdomain.com/.well-known/mercure
    MERCURE_JWT_SECRET=your_mercure_secret
    NUXT_PUBLIC_CWA_API_URL=http://php               # server-side (internal Docker network)
    NUXT_PUBLIC_CWA_API_URL_BROWSER=https://api.yourdomain.com  # client-side (external)
    MAILER_DSN=smtp://user:pass@smtp:1025

- Starting the stack:
    docker compose up -d
    docker compose exec php bin/console doctrine:migrations:migrate
    docker compose exec php bin/console silverback:api-components:user:create

- Development hot-reload:
    The docker-compose.override.yml mounts your source into the container
    Nuxt: hot module replacement works in dev mode
    PHP: changes on the host are reflected immediately (no build step for PHP)

- Building for production:
    docker compose build
    — Multi-stage Dockerfiles: deps → builder → runner stages
    — PHP Dockerfile: Composer install, migrations are NOT run at build time
    — Nuxt Dockerfile: pnpm install + nuxt build (outputs .output/)
    — Image tags: tag with git SHA for immutable deploys

- Common gotchas:
    - NUXT_PUBLIC_CWA_API_URL vs NUXT_PUBLIC_CWA_API_URL_BROWSER:
        Must be different when your API is on an internal Docker hostname
    - JWT keys: generate once, mount as a secret (never bake into the image)
    - Database: run migrations as a step before starting PHP, not in the entrypoint
-->
