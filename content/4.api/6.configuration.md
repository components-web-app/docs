---
title: Configuration Reference
description: Complete reference for all silverback_api_components bundle configuration options.
---

<!--
COVER:
- Full YAML structure with every option, its type, default, and description:

silverback_api_components:
    user:
        class_name: App\Entity\User   # required; your User entity class

    publishable:
        permission: "is_granted('ROLE_ADMIN')"  # expression language; who can access draft resources

    enabled_components:
        collection: true    # enable/disable the built-in Collection component
        form: true          # enable/disable the built-in Form component

    refresh_token:
        handler_id: silverback.api_components.refresh_token.storage.doctrine
        options:
            class: App\Entity\RefreshToken  # your RefreshToken entity (from recipe)
        cookie_name: api_components         # must match lexik_jwt cookie name
        ttl: 604800                         # seconds; default 1 week

    mailer:
        # ... email notification config options

- Environment variables used by the bundle (consolidated reference):
    DATABASE_URL
    JWT_SECRET_KEY, JWT_PUBLIC_KEY, JWT_PASSPHRASE
    MERCURE_URL, MERCURE_PUBLIC_URL, MERCURE_JWT_SECRET
    MAILER_DSN
    APP_SECRET (Symfony)

- Console commands provided by the bundle:
    bin/console silverback:api-components:user:create  — create an admin user
    bin/console silverback:api-components:... — list any other commands
    — describe each command's arguments and options

- Services you can override/decorate:
    - FilesystemProvider: for custom storage adapters
    - The UserChecker service
    - Auth success/failure handlers
-->
