---
title: Bundle Setup
description: Installing and configuring the API Components Bundle in a Symfony application.
---

<!--
COVER:
- Prerequisites: Symfony 6+, API Platform 3, Doctrine ORM, PHP 8.1+
- Installation via Composer:
    composer require silverback/api-components-bundle
  - The Flex recipe creates: App\Entity\User, security.yaml skeleton, config/packages/silverback_api_components.yaml

- Required bundles the recipe wires up:
    - LexikJWTAuthenticationBundle (auth)
    - gesdinet/jwt-refresh-token-bundle (refresh tokens)
    - vich/uploader-bundle (file uploads, optional)
    - liip/imagine-bundle (image processing, optional)

- Minimum bundle config (config/packages/silverback_api_components.yaml):
    silverback_api_components:
        user:
            class_name: App\Entity\User

- JWT key generation (covered in detail in Users & Security):
    mkdir -p config/jwt
    openssl genpkey ... / openssl pkey ...
  - Point to .env.local for the passphrase

- Database setup:
    bin/console doctrine:migrations:diff
    bin/console doctrine:migrations:migrate
  - What migrations the bundle creates: layout, page, route, component_group, component_position, refresh_token, user, media_object tables

- The API entrypoint: what /_/entrypoint returns (all resource collection IRIs, docs link)
- API docs: /docs (Swagger UI), /docs.jsonld (JSON-LD)
- Health check endpoint: GET /healthcheck

- Verifying the install: loading fixtures
    bin/console doctrine:fixtures:load
  - What the fixtures create: a primary layout, a home page, a home route

- Environment variables reference:
    DATABASE_URL
    JWT_SECRET_KEY / JWT_PUBLIC_KEY / JWT_PASSPHRASE
    MERCURE_URL / MERCURE_PUBLIC_URL / MERCURE_JWT_SECRET
    MAILER_DSN (for email verification)
-->
